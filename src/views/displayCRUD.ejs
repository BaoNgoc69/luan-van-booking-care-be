<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get CRUD</title>
</head>
<style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    td,
    th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }

    .btn-edit {
        border: none;
        border-radius: 3px;
        outline: none;
        height: 30px;
        cursor: pointer;
        background: orange;
        text-decoration: none;
        padding: 5px;
    }

    .btn-delete {
        border: none;
        border-radius: 3px;
        outline: none;
        height: 30px;
        cursor: pointer;
        background: rgb(214, 62, 62);
        text-decoration: none;
        padding: 5px;
    }
</style>

<body>
    <table>
        <tr>
            <th>Email</th>
            <th>FirstName</th>
            <th>LastName</th>
            <th>Address</th>
            <th>Addsion</th>


        </tr>


        <% for (let i=0; i <dataTable.length; i++ ) { %>
            <tr>
                <td>
                    <%= dataTable[i].email %>
                </td>
                <td>
                    <%= dataTable[i].firstName %>
                </td>
                <td>
                    <%= dataTable[i].lastName %>
                </td>
                <td>
                    <%= dataTable[i].address %>
                </td>
                <td>
                    <a href="/edit-crud?id=<%= dataTable[i].id %>" type="button" class="btn-edit">Edit</a>
                    <a href="/delete-crud?id=<%= dataTable[i].id %>" type="button" class="btn-edit" type="button"
                        class="btn-delete">Delete</a>

                </td>

            </tr>
            <% } %>


    </table>

</body>
</body>

</html>